# Dependencies
node_modules/

# Build outputs
dist/
.next/
out/
build/

# Environment & secrets (.env.example stays tracked)
.env
.env.*
!.env.example

# Logs & runtime
logs/
*.log
.pnpm-debug.log*
.turbo

# Python (inference / tooling)
**/__pycache__/
**/*.py[cod]
**/.venv/
venv/
*.egg-info/

# OS / editors
.DS_Store
Thumbs.db
.idea/
*.swp
*.swo

# Test / coverage
coverage/
.nyc_output/
*.tsbuildinfo

# Deploy / TLS misc
*.pem
.vercel

# One-off local artifacts
apps/server/review-output.txt

# Lighthouse / perf scratch dumps (repo root)
.tmp-lh-*.json

# Packaged extension artifact (use npm run zip:ext)
/extension.zip

# Inference app markdown (keep locally; not part of code commits)
apps/inference/**/README.md

# Local prompt / design pack (not part of the app)
komposo-prompts-*/

# Clerk dev cache / keyless (never commit)
apps/client/.clerk/

# GitHub rejects files >100MB — keep locally or use Git LFS / object storage
**/*.safetensors
apps/server/src/modules/seeding/data/Wellfound_Final.csv

# Local bulk exports (keep on disk; do not commit or overwrite on sync)
data/company-dataset/Wellfound_final
data/company-dataset/Wellfound_final/
apps/server/data/company-dataset/Wellfound_final
apps/server/data/company-dataset/Wellfound_final/
apps/server/data/company-datasets/Wellfound_Final.csv
# Local dataset shards / smoke CSVs (keep on disk; do not commit)
apps/server/data/company-datasets/companies-*.csv
# Generated chunks (optional; re-run split:company-csv locally)
apps/server/data/company-datasets/split/
