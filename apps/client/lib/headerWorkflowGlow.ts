/** Dispatched when the header workflow path reaches the account endpoint. */
export const WORKFLOW_GLOW_EVENT = "jobseek:workflowGlow";

/** Fixed account avatar / sign-in control — squiggle ends here (desktop `lg+`). */
export const WORKFLOW_ACCOUNT_ENDPOINT_ID = "workflow-account-endpoint";

/** Glow duration when the path completes (ms). */
export const WORKFLOW_GLOW_MS = 1700;
