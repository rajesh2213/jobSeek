Place your trained HuggingFace model here (tokenizer + `config.json` + weights), e.g. contents of a saved `AutoTokenizer` + `AutoModelForSequenceClassification` directory.

The inference service loads this path by default (`apps/inference/models/job-parser-model`).
